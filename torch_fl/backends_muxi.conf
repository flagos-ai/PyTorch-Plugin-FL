# flagos op backend config
# Format: op_name = backend
# backend: flaggems | cuda
# Default (unlisted ops): flaggems
#
# Override config path: FLAGOS_BACKEND_CONFIG=/path/to/backends.conf

mm = flaggems
mm.out = flaggems
bmm = flaggems
bmm.out = flaggems
cat = cuda
embedding = flaggems
add.Tensor = maca
mul.Tensor = maca
silu = cuda
rsqrt = cuda
mean.dim = cuda
cos = cuda
sin = cuda
neg = cuda
pow.Tensor_Scalar = cuda
all = maca
_softmax = cuda
bitwise_and.Tensor = cuda
le.Tensor = maca
where.self = cuda
index.Tensor = cuda
new_ones = cuda
scalar_tensor = cuda
ones_like = cuda
zeros = cuda
silu_backward = cuda
sum.dim_IntList = cuda
slice_backward = cuda
constant_pad_nd = cuda
embedding_dense_backward = cuda
nll_loss_forward = cuda
nll_loss_backward = cuda

# flagos op backend config
# Format: op_name = backend
# backend: flaggems | flagos_python | cuda
# Default (unlisted ops): flaggems
#
# Override config path: FLAGOS_BACKEND_CONFIG=/path/to/backends.conf

mm = cuda
mm.out = cuda
bmm = cuda
bmm.out = cuda
cat = cuda
embedding = cuda
add.Tensor = cuda
mul.Tensor = cuda
silu = cuda
rsqrt = cuda
mean.dim = cuda
cos = cuda
sin = cuda
neg = cuda
pow.Tensor_Scalar = cuda
all = cuda
_softmax = cuda
bitwise_and.Tensor = cuda
le.Tensor = cuda
where.self = cuda
index.Tensor = cuda
new_ones = cuda
scalar_tensor = cuda
ones_like = cuda
zeros = cuda
silu_backward = cuda
sum.dim_IntList = cuda
slice_backward = cuda
constant_pad_nd = cuda
embedding_dense_backward = cuda
nll_loss_forward = cuda
nll_loss_backward = cuda
abs = cuda
acos = cuda
sort = cuda
topk = cuda
multinomial = cuda
argmax = cuda
argmin = cuda
fill_.Scalar = cuda
arange = cuda
arange.start_step = cuda
sub.Tensor = cuda
eq.Tensor = cuda
eq.Scalar = cuda
max = cuda
bitwise_not = cuda
bitwise_or.Tensor = cuda
any = cuda
any.dim = cuda
isin.Tensor_Tensor = cuda
masked_fill_.Scalar = cuda
lt.Tensor = cuda
lt.Scalar = cuda
cumsum = cuda

# log_softmax and softmax backward ops
_log_softmax = cuda
_log_softmax_backward_data = cuda
_softmax_backward_data = cuda

# div
div.Scalar = cuda

# _foreach_* optimizer ops
_foreach_mul_.Scalar = cuda
_foreach_add_.Scalar = cuda
_foreach_addcdiv_.ScalarList = cuda
_foreach_addcmul_.Scalar = cuda
_foreach_lerp_.Scalar = cuda
_foreach_div_.ScalarList = cuda
_foreach_add_.List = cuda
_foreach_mul_.List = cuda
_foreach_sqrt = cuda
_foreach_neg = cuda
_foreach_reciprocal = cuda
